const directorySelectors = require("./directoryList");
const quoteSelectors = require("./quote");
const pagerSelectors = require("./pager");

module.exports = {
 directorySelectors,
 quoteSelectors,
 pagerSelectors
};
